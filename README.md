
  # so pra motos

  This is a code bundle for so pra motos. The original project is available at https://www.figma.com/design/MfNhoNxQnSAMntpWoCsoV0/so-pra-motos.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  